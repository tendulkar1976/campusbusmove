{
  "rules": {
    "routes": {
      "$routeId": {
        "live": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
